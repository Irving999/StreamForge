services:
  api:
    build: .
    command: uvicorn server:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    env_file:
      - .env

  worker:
    build: .
    command: python -u worker.py
    env_file:
      - .env